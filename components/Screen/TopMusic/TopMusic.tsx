import React from "react";
import { View, Text } from "react-native";
import styles from "./style";
const TopMusic = () => {
    return(
        <View style={styles.container}>
            <Text>Top music Screen</Text>
        </View>
    );
};

export default TopMusic;